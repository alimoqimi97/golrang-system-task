import { User } from "types";

// export const crawl = (user: User, allValues?: any[]) => {
//   if (!allValues) allValues = [];
//   for (var key in user) {
//     if (typeof user[key] === "object") crawl(user[key], allValues);
//     else allValues.push(user[key] + " ");
//   }
//   return allValues;
// };
